# Manual JNI surface — Microsoft.AndroidX.Compose

Generated by `scripts/manual-jni-report.cs` on 2026-06-09 18:09 UTC.

Source of truth: `src/Microsoft.AndroidX.Compose/*.cs`. The script flags 
every C# member that calls `JNIEnv.*` directly, plus every Java 
Callable Wrapper the project registers under `net/compose/...`. 
Members whose bodies are emitted by the source generators 
(`[ComposeBridge]`, `[ComposeFacade]`) are deliberately excluded — 
the report measures **what's left for the generator to absorb**.

## Summary

- C# files scanned: **255**
- Files containing manual JNI or a JCW: **47**
- Manual `JNIEnv.*` call sites: **196**
- Lines inside flagged members: **1485**
- Java Callable Wrappers (`[Register("net/compose/…")]`): **25** (4 with raw JNI inside, 21 pure-managed delegate adapters)
- Generator-emitted `[ComposeBridge]` partials: **202**
- Generator-emitted `[ComposeFacade]` partials: **95**
- Manual JNI : generator-emitted ratio: **0.66** (`196` raw-JNI calls vs `297` generator-emitted partials)

## Per-file detail

### `src/Microsoft.AndroidX.Compose/Alignment.cs`

**Purpose.** C# wrapper around the androidx.compose.ui.Alignment singletons exposed by Kotlin's Alignment.Companion object. Each public static property forwards to the bound AndroidX.Compose.UI.IAlignment.Companion singleton and caches the wrapper fo...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `From` | method | 35–39 | 1 | Wrap the bound IAlignment peer's JNI handle in a brand-new Java.Lang.Object peer so casts to Java.Lang.Object on this instance return our own (stable) handle. Same approach as Shape. | TODO |

### `src/Microsoft.AndroidX.Compose/ComposableLambda0.cs`

**Purpose.** Function0&lt;Unit&gt; — zero-arg callbacks (Button.onClick, etc.). A [Register]'d Java.Lang.Object ACW so Compose's bytecode-typed lambda parameter accepts it.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda0Int.cs`

**Purpose.** Function0&lt;Integer&gt; — a no-arg Kotlin lambda returning a boxed `Integer`. Used for state-holder factories like rememberCarouselState(itemCount: () -&gt; Int) / rememberLazyListState(initialFirstVisibleItemIndex: () -&gt; Int) that r...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda1.cs`

**Purpose.** Function1&lt;T, Unit&gt; — single-arg callbacks (TextField's onValueChange, onCheckedChange, etc.). The body receives the raw Java arg and is responsible for unboxing it.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda2.cs`

**Purpose.** Function2&lt;Composer, Integer, Unit&gt; — top-level composition + theme/scope content. p0 is the composer, p1 is $changed.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda3.cs`

**Purpose.** Function3&lt;Scope, Composer, Integer, Unit&gt; — Column/Row/Box/Button content. p0 is the receiver scope (RowScope/ColumnScope), p1 is the composer, p2 is $changed. Three ctors: Action&lt;IComposer&gt; discards the scope — used wherever...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda4.cs`

**Purpose.** Function4&lt;Scope, T, Composer, Integer, Unit&gt; — the shape Compose's LazyListScope.items and LazyGridScope.items use for their per-item content lambda. p0 is the lazy item / grid item scope (raw `IntPtr`), p1 is the boxed per-item va...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/Compose.cs`

**Purpose.** Top-level composition utilities that need to be callable from anywhere inside a composition pass — not just from `ComposeActivity` subclasses. Use these from extracted helper composables, custom `ComposableNode` subclasses, etc.

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `RememberSaveableScalar` | method | 221–246 | 2 | TODO: document | TODO |
| `RememberSaveableWrapper` | method | 248–295 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/ComposeBridges.cs`

**Purpose.** Raw-JNI bridges to Compose functions the .NET-for-Android binding generator can't see (Compose @Composable functions don't get $default sibling overloads — the trailing $default bitmask lives on the regular method). The bodies for every ...

_File also contributes 195 `[ComposeBridge]` + 92 `[ComposeFacade]` generator-emitted partials, excluded from this section._

**Manual entry points** (11):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `ModifierCompanionInstance` | method | 39–52 | 6 | TODO: document | TODO |
| `ModifierClipRoundedCorners` | method | 215–227 | 1 | Convenience wrapper that composes RoundedCornerShape + ModifierClip and manages the intermediate Shape local ref. Listed as out of scope in #36 — the underlying JNI calls are both generator-driven now; only the two-step orchestration sta... | see #36 |
| `BoxScopeAlign` | method | 2386–2399 | 3 | TODO: document | TODO |
| `BoxScopeMatchParentSize` | method | 2401–2414 | 3 | TODO: document | TODO |
| `RowScopeAlignVertical` | method | 2419–2432 | 3 | TODO: document | TODO |
| `ColumnScopeAlignHorizontal` | method | 2437–2450 | 3 | TODO: document | TODO |
| `FocusRequesterRequestFocus` | method | 2460–2469 | 3 | TODO: document | TODO |
| `ExposedDropdownMenu` | method | 2907–2959 | 3 | TODO: document | TODO |
| `RememberNavController` | method | 3671–3702 | 7 | TODO: document | TODO |
| `ModifierPointerInput` | method | 3760–3781 | 3 | androidx.compose.ui.input.pointer.SuspendingPointerInputFilterKt .pointerInput(Modifier, Object key1, PointerInputEventHandler): Modifier Hand-written because the [ComposeBridge] generator doesn't know about non-bound interface types lik... | TODO |
| `NewPointerInputEventHandler` | method | 3797–3813 | 3 | net.compose.PointerInputEventHandlerImpl.<init>(Function2) Allocates the Java-side helper (shipped via <AndroidJavaSource>) that implements the bound but otherwise-unreachable PointerInputEventHandler interface by forwarding invok... | TODO |

### `src/Microsoft.AndroidX.Compose/ContentScale.cs`

**Purpose.** C# wrapper around the androidx.compose.ui.layout.ContentScale singletons exposed by Kotlin's ContentScale.Companion object. Compose's ContentScale is a real Kotlin interface (not a @JvmInline value class) — same plumbing story as `TextDe...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Resolve` | method | 24–31 | 3 | TODO: document | TODO |
| `Companion` | method | 38–49 | 5 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/DisposableEffectBody.cs`

**Purpose.** JCW for Compose's DisposableEffect body — Function1&lt;DisposableEffectScope, DisposableEffectResult&gt;. Kotlin invokes the body once per (key change | enter composition) and stores the returned `IDisposableEffectResult`; when keys chan...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/DragAndDropTarget.cs`

**Purpose.** androidx.compose.ui.draganddrop.DragAndDropTarget adapter — the receiver of drag-and-drop callbacks installed by `DragAndDropTarget`. Mirrors Kotlin's DragAndDropTarget interface as a delegate-bag: `OnDrop` processes the dropped payload ...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/DrawerValueConfirmStateChange.cs`

**Purpose.** Function1&lt;DrawerValue, Boolean&gt; adapter used as the confirmStateChange callback when constructing a DrawerState via rememberDrawerState. The callback is part of the remember cache key in Kotlin, so this type is allocated <strong>on...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/FontFamily.cs`

**Purpose.** C# wrapper around androidx.compose.ui.text.font.FontFamily. Compose's FontFamily is a real Kotlin class, but Compose 1.11.2.1's ui-text-android package is shipped as a Java-library-only stub with zero exported types — so the .NET binding...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Companion` | method | 33–44 | 5 | TODO: document | TODO |
| `Resolve` | method | 46–60 | 3 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/FontStyle.cs`

**Purpose.** C# wrapper around androidx.compose.ui.text.font.FontStyle. In Kotlin source, FontStyle is a @JvmInline value class wrapping an Int, but every @Composable function in Material 3 declares it as <em>nullable</em> (fontStyle: FontStyle? = nu...

**Manual entry points** (3):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Companion` | method | 31–42 | 5 | TODO: document | TODO |
| `BoxMethod` | method | 44–52 | 2 | TODO: document | TODO |
| `Resolve` | method | 54–65 | 5 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/FontWeight.cs`

**Purpose.** C# wrapper around androidx.compose.ui.text.font.FontWeight. Compose's FontWeight is a real Kotlin class (not a value class), but Compose 1.11.1's ui-text-android package is shipped as a Java-library-only stub with zero exported types — s...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Companion` | method | 31–42 | 5 | TODO: document | TODO |
| `Resolve` | method | 44–51 | 3 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/Icon.cs`

**Purpose.** Material 3 Icon — renders a small image used for affordance, typically inside a `Button`, `IconButton`, or a `NavigationBarItem`. Three source types are supported: An `ImageVector` obtained from a Compose icon library — uses the directly...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Render` | method | 63–137 | 1 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/IntCallback.cs`

**Purpose.** JCW adapter implementing Function1&lt;Integer, Integer&gt;, used by the slideIn* / slideOut* transition factories whose offset parameter is a Kotlin (Int) -&gt; Int lambda mapping the container's measured width/height to the slide's init...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/JobCompletionHandler.cs`

**Purpose.** JCW for Function1&lt;Throwable?, Unit&gt; registered via `InvokeOnCompletion(bool, bool, IFunction1`. Compose invokes this handler when the launched coroutine's job transitions into the cancelling state, so we can propagate cancellation ...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/KotlinResult.cs`

**Purpose.** Raw-JNI helpers for Kotlin's kotlin.Result type — the boxed success/failure wrapper Kotlin coroutines pass through Continuation.resumeWith.

**Manual entry points** (5):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `CreateFailure` | method | 79–101 | 1 | Construct a kotlin.Result.Failure(throwable) instance — the boxed-failure form a `IContinuation` expects when something goes wrong inside its suspend body. The Java throwable to wrap; non-null. C# exceptions can be converted via `JavaPro... | TODO |
| `IsFailure` | method | 112–117 | 1 | Returns <see langword="true"/> if `handle` refers to a kotlin.Result.Failure instance — i.e. the resumed value represents a failure rather than a success. Raw JNI handle to test. `Zero` is treated as "not a failure" (a null resume value ... | TODO |
| `ExtractException` | method | 133–153 | 1 | Extract the underlying `Throwable` from a kotlin.Result.Failure peer. A peer wrapping a Result.Failure handle. Must have been vetted with `IntPtr` first. <returns> The wrapped throwable as a `Exception` (`Throwable` derives from `Excepti... | TODO |
| `EnsureResultKt` | method | 155–163 | 2 | TODO: document | TODO |
| `EnsureResultFailure` | method | 165–171 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/LambdaViewModelFactory.cs`

**Purpose.** Internal JCW adapter that exposes a `Func{T}` as an `IFactory` so `ViewModelProvider` can construct the view model the first time the host's `ViewModelStore` doesn't already contain the requested key.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/LaunchedEffectBody.cs`

**Purpose.** JCW for the suspend lambda Kotlin's LaunchedEffect expects: Function2&lt;CoroutineScope, Continuation&lt;in Unit&gt;, Any?&gt;. Bridges the Kotlin coroutine to a C# async Task body that accepts a `CancellationToken`.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/LinkClickListener.cs`

**Purpose.** androidx.compose.ui.text.LinkInteractionListener adapter that forwards Compose's link-tap callbacks to a C# `Action{T}`. Allocated once per `Clickable` call and held by the resulting `LinkAnnotation` so the JNI peer stays alive as long a...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ManagedBox.cs`

**Purpose.** Wraps an arbitrary managed value as a `Object` so it can cross the JNI boundary. Used by `CompositionLocal{T}` to hand pure-C# POCO values (themes, settings, custom contexts) to Kotlin's compositionLocalOf / staticCompositionLocalOf mach...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/Modifier.cs`

**Purpose.** C# mirror of Kotlin's androidx.compose.ui.Modifier chain. Build a chain by calling fluent methods on `Companion` — each call returns a NEW immutable `Modifier` with the op appended. On a container (anything deriving from `ComposableConta...

**Manual entry points** (3):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Border` | method | 282–303 | 1 | Modifier.border(width, color, RoundedCornerShape(cornerRadius)) — draws a stroke with rounded corners. Match `cornerRadius` to a `Clip(Dp` earlier in the chain so the corners align (otherwise the rectangular stroke gets sliced by a round... | TODO |
| `DetectTapGestures` | method | 1229–1280 | 1 | Modifier.pointerInput(Unit) { detectTapGestures(...) } — detect the four basic single-pointer tap gestures (tap, press, long-press, double-tap) and invoke the supplied C# callbacks on the UI thread with the tap position as an `Offset` in... | TODO |
| `Build` | method | 1369–1407 | 3 | Materialize the chain into a managed IModifier wrapper, optionally prepending a Modifier.padding(contentPadding) op against the supplied `IntPtr` handle. The padding op runs FIRST, before any user ops — semantically equivalent to Modifie... | see #46 |

### `src/Microsoft.AndroidX.Compose/NavGraphBuilderLambda.cs`

**Purpose.** Function1&lt;NavGraphBuilder, Unit&gt; — the builder argument to NavHostKt.NavHost(navController, startDestination, modifier, route, builder, ...). NOT a @Composable lambda: NavHost invokes it synchronously during graph construction, bef...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/NoOpSearchCallback.cs`

**Purpose.** Function1&lt;String, Unit&gt; stub that ignores its argument and returns null. Supplied to SearchBarDefaults.InputField.onSearch when the C# caller did not provide an onSearch callback — the Kotlin API does not tolerate a null callback (...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ObjectFunction0.cs`

**Purpose.** JCW that implements Kotlin's Function0&lt;Object&gt; — a zero-argument lambda returning a boxed managed object. Unlike `ComposableLambda0`, which returns Unit.INSTANCE (void in Kotlin terms), this one returns a typed value Compose / the ...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/OffsetCallback.cs`

**Purpose.** JCW adapter implementing Function1&lt;Offset, Unit&gt; for gesture-detector callbacks like onTap / onLongPress / onDoubleTap. Because Kotlin's Offset is a @JvmInline value class, the boxed argument p0 arrives as an androidx.compose.ui.ge...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Invoke` | method | 24–34 | 1 | TODO: document | TODO |
| `EnsureUnboxMethod` | method | 36–41 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/OffsetPressCallback.cs`

**Purpose.** JCW adapter implementing Function3&lt;PressGestureScope, Offset, Continuation, Object&gt; for the onPress callback of detectTapGestures. Kotlin lowers the original suspend PressGestureScope.(Offset) -&gt; Unit shape to a Function3 with t...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Invoke` | method | 40–50 | 1 | TODO: document | TODO |
| `EnsureUnboxMethod` | method | 52–57 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/PointerInputBlock.cs`

**Purpose.** JCW exposing a `IFunction2` that Compose's pointerInput(key, handler) invokes inside the gesture-detector coroutine. The Java-side net.compose.PointerInputEventHandlerImpl (shipped via &lt;AndroidJavaSource&gt;) implements the bou...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Invoke` | method | 66–116 | 2 | TODO: document | TODO |
| `EnsureSuspendedHandle` | method | 118–127 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/RememberHolder.cs`

**Purpose.** Single-field `Object` wrapper that lets `Func{T}, int, string` store an arbitrary managed T (often a `MutableState{T}`) in Compose's slot table, which only holds `Object` references. Registered with the .NET-for-Android peer cache ([Regi...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/SaveableBridges.cs`

**Purpose.** Hand-written raw-JNI bridges for the `androidx.compose.runtime.saveable` package. The Xamarin NuGet (`Xamarin.AndroidX.Compose.Runtime.Saveable.Android`) ships the Java library but does not generate C# bindings, so every Saveable entry p...

**Manual entry points** (5):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `SaverAutoSaver` | method | 36–60 | 5 | androidx.compose.runtime.saveable.SaverKt.autoSaver(): Saver<T, Object> The default `Saver` Compose uses for any `Bundle`-saveable value (primitives, strings, parcelables, …). Cached as a global ref — returns the same singleton on every ... | TODO |
| `EmptyObjectArray` | method | 68–84 | 3 | Lazily-cached empty `Object[]` for the `vararg inputs` parameter of `rememberSaveable`. Compose treats inputs as "dependency tracking" — if any element changes, the cached factory result is discarded. We always pass an empty array (= "ne... | TODO |
| `RememberSaveableSimple` | method | 95–127 | 3 | androidx.compose.runtime.saveable.RememberSaveableKt.rememberSaveable( Object[] inputs, Function0<? extends T> init, Composer composer, int $changed): Object The no-saver overload — Compose internally uses `autoSaver()`, which handles ev... | TODO |
| `RememberSaveableMutableState` | method | 144–178 | 3 | androidx.compose.runtime.saveable.RememberSaveableKt.rememberSaveable( Object[] inputs, Saver<T, ?> stateSaver, Function0<? extends MutableState<T>> init, Composer composer, int $changed): MutableState<T> The MutableState overload — Comp... | TODO |
| `BuildKeysArray` | method | 192–218 | 3 | Builds a Kotlin-compatible `Object[]` from a managed `object?[]` for the `inputs` (vararg) parameter of `rememberSaveable`. Each element is boxed via the same primitive→Java.Lang.Object switch MutableState<T> uses, so primitives compare ... | TODO |

### `src/Microsoft.AndroidX.Compose/Shape.cs`

**Purpose.** C# wrapper around androidx.compose.ui.graphics.Shape. Compose's Shape is an interface; concrete shapes like RoundedCornerShape live in ui-graphics-android, which is bound by <see href="https://github.com/dotnet/android-libraries/commit/d...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `RoundedCorners` | method | 49–55 | 1 | androidx.compose.foundation.shape.RoundedCornerShape(topStart, topEnd, bottomEnd, bottomStart) — independent radii per corner expressed in `Dp`. The argument order matches Kotlin: top-start, top-end, bottom-end, bottom-start (clockwise f... | TODO |

### `src/Microsoft.AndroidX.Compose/ShapesBridges.cs`

**Purpose.** Build a Material 3 `Shapes` with per-slot overrides. Each `IntPtr` is either a CornerBasedShape JNI handle to use for that slot, or `Zero` to fall back to the M3 baseline default for that slot via Kotlin's synthetic $default mechanism.

_File also contributes 1 `[ComposeBridge]` generator-emitted partials, excluded from this section._

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `BuildShapes` | method | 46–71 | 3 | Build a Material 3 `Shapes` with per-slot overrides. Each `IntPtr` is either a CornerBasedShape JNI handle to use for that slot, or `Zero` to fall back to the M3 baseline default for that slot via Kotlin's synthetic $default mechanism. 5... | TODO |

### `src/Microsoft.AndroidX.Compose/ShouldStartDragAndDropCallback.cs`

**Purpose.** JCW adapter for the `shouldStartDragAndDrop: (DragAndDropEvent) -> Boolean` parameter on `Modifier.dragAndDropTarget(...)`. Wraps a managed `Func<DragAndDropEvent, bool>` and returns a boxed Java.Lang.Boolean so Kotlin's Function1 contra...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/SnapshotFlowContinuation.cs`

**Purpose.** JCW implementing Kotlin's `IContinuation` for the long-running Flow.collect call that drives `Func{T}`. Unlike `SuspendContinuation`, which models a one-shot suspend resumed back into a `TaskCompletionSource{TResult}`, this continuation:...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/SuspendBridge.cs`

**Purpose.** Shared plumbing for calling Kotlin suspend functions from C# async/await. Each call allocates one `SuspendContinuation`, hands it to the Kotlin function, and either: completes synchronously when Kotlin returns anything other than the COR...

**Manual entry points** (3):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Invoke` | method | 81–159 | 1 | Call a Kotlin suspend function and project its boxed result through `unbox` into a typed `Task{TResult}`. Invokes the Kotlin suspend function with the supplied `SuspendContinuation` as the trailing argument and returns either the boxed r... | TODO |
| `IsCoroutineSuspended` | method | 204–209 | 1 | TODO: document | TODO |
| `EnsureSuspendedHandle` | method | 211–220 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/SuspendBridges.cs`

**Purpose.** Hand-written JNI bridges for Kotlin `suspend` functions that the `[ComposeBridge]` source generator doesn't yet model. v1 ships just the ScrollState entries (`scrollTo` is already exposed via the regular C# binding, so only `animateScrol...

_File also contributes 1 `[ComposeBridge]` generator-emitted partials, excluded from this section._

**Manual entry points** (8):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `ScrollStateScrollTo` | method | 32–60 | 3 | androidx.compose.foundation.ScrollState.scrollTo(int value, Continuation): Object The two-arg suspend overload is exposed in the binding (returns Java.Lang.Object), but we call it via raw JNI here so the returned handle stays as a plain ... | TODO |
| `ScrollStateAnimateScrollTo` | method | 89–124 | 3 | androidx.compose.foundation.ScrollState.animateScrollTo( int value, AnimationSpec<Float> = SpringSpec(), Continuation): Object The two-arg overload (no `animationSpec`) is stripped from the binding, so we invoke the synthetic `$default` ... | see dotnet/java-interop#1440 |
| `DrawerStateOpen` | method | 133–155 | 3 | androidx.compose.material3.DrawerState.open(Continuation): Object The single-arg suspend wrapper is exposed in the binding (returns Java.Lang.Object), but we call it via raw JNI so the returned handle stays as a plain IntPtr — SuspendBri... | TODO |
| `DrawerStateClose` | method | 160–182 | 3 | androidx.compose.material3.DrawerState.close(Continuation): Object Mirror of DrawerStateOpen for the close-drawer suspend call. | TODO |
| `AndroidUiDispatcherMain` | method | 198–229 | 9 | androidx.compose.ui.platform.AndroidUiDispatcher.Companion.getMain(): kotlin.coroutines.CoroutineContext Returns the singleton CoroutineContext that combines a main-thread dispatcher with a MonotonicFrameClock backed by Choreographer. Re... | TODO |
| `LazyListStateScrollToItem` | method | 244–269 | 3 | androidx.compose.foundation.lazy.LazyListState .scrollToItem(int index, int scrollOffset, Continuation): Object The three-arg suspend overload is bindable (returns Java.Lang.Object), but we call it via raw JNI so the returned handle stay... | TODO |
| `LazyListStateAnimateScrollToItem` | method | 279–304 | 3 | androidx.compose.foundation.lazy.LazyListState .animateScrollToItem(int index, int scrollOffset, Continuation): Object Animation suspend method. Requires a MonotonicFrameClock in the continuation context (calls `withFrameNanos` internall... | TODO |
| `DetectTapGestures` | method | 335–376 | 3 | androidx.compose.foundation.gestures.TapGestureDetectorKt .detectTapGestures( PointerInputScope scope, ((Offset) -> Unit)? onDoubleTap = null, ((Offset) -> Unit)? onLongPress = null, (suspend PressGestureScope.(Offset) -> Unit) onPress =... | TODO |

### `src/Microsoft.AndroidX.Compose/SuspendContinuation.cs`

**Purpose.** Java Callable Wrapper that lets a Kotlin suspend function resume back into managed code via a `TaskCompletionSource{TResult}`. The wrapped kotlin.Result&lt;T&gt; handed to `Object` is stored verbatim and unboxed by `SuspendBridge`.

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `CompleteWithLocalHandle` | method | 160–177 | 2 | Funnel a raw JNI local ref into the TCS. Promotes the handle to a global ref, wraps it in a managed peer with `TransferGlobalRef`, and stores the peer in `Tcs`. Local ref to the boxed result, or IntPtr.Zero. When true, the supplied local... | TODO |

### `src/Microsoft.AndroidX.Compose/TextAlign.cs`

**Purpose.** C# wrapper around androidx.compose.ui.text.style.TextAlign. In Kotlin source, TextAlign is a @JvmInline value class wrapping an Int, but every @Composable function in Material 3 declares it as <em>nullable</em> (textAlign: TextAlign? = n...

**Manual entry points** (3):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Companion` | method | 33–44 | 5 | TODO: document | TODO |
| `BoxMethod` | method | 46–54 | 2 | TODO: document | TODO |
| `Resolve` | method | 56–69 | 5 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/TextDecoration.cs`

**Purpose.** C# wrapper around androidx.compose.ui.text.style.TextDecoration. Compose's TextDecoration is a real Kotlin class (not a value class), with companion constants for None, Underline, and LineThrough. Same plumbing story as `FontWeight` — ui...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Companion` | method | 25–36 | 5 | TODO: document | TODO |
| `Resolve` | method | 38–45 | 3 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/TextFieldValueBridges.cs`

**Purpose.** JNI bridge for the one piece of androidx.compose.ui.text.input.TextFieldValue that the binder strips: the constructor. Everything else — Text/Selection/Composition/AnnotatedString properties, Copy(text, selection, composition) overloads,...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `EnsureTextFieldValueClass` | method | 38–44 | 2 | TODO: document | TODO |
| `NewTextFieldValueImpl` | method | 49–71 | 3 | Construct a Kotlin TextFieldValue and return the bound binding wrapper. Always invokes the synthetic default-mask ctor so we can pass null for composition without knowing which non-synthetic overloads exist. | TODO |

### `src/Microsoft.AndroidX.Compose/TextStyleCompanion.cs`

**Purpose.** Cached accessor for androidx.compose.ui.text.TextStyle.Companion — the singleton that exposes Default (and via that Default.toSpanStyle()). The C# binding doesn't expose a static accessor for the Companion field, so we read it through JN...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Get` | method | 20–34 | 5 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/ThemeBridges.cs`

**Purpose.** Build a Material 3 light `ColorScheme` using every per-slot default from the M3 tonal palette tokens.

_File also contributes 1 `[ComposeBridge]` generator-emitted partials, excluded from this section._

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `InvokeColorSchemeDefault` | method | 98–139 | 3 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/TopAppBarDefaults.cs`

**Purpose.** Static factories mirroring Kotlin's androidx.compose.material3.TopAppBarDefaults singleton — each returns an `ITopAppBarScrollBehavior` configured for a particular collapse strategy.

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Invoke` | method | 157–188 | 1 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/TypographyBridges.cs`

**Purpose.** Build a Material 3 `Typography` with per-slot overrides. Each `IntPtr` is either a TextStyle JNI handle to use for that slot, or `Zero` to fall back to the M3 baseline default for that slot via Kotlin's synthetic $default mechanism.

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `BuildTypography` | method | 51–96 | 3 | Build a Material 3 `Typography` with per-slot overrides. Each `IntPtr` is either a TextStyle JNI handle to use for that slot, or `Zero` to fall back to the M3 baseline default for that slot via Kotlin's synthetic $default mechanism. 15-b... | TODO |

## JCW classes

Every Microsoft.AndroidX.Compose Java Callable Wrapper — classes registered under 
the project's own `net/compose/` JNI namespace. They lower C# delegates 
to Kotlin `IFunctionN` shapes the binding generator can't synthesize.

| Class | Implements | File:line | Has raw JNI? |
| --- | --- | --- | --- |
| `ComposableLambda0` | `IFunction0` | `src/Microsoft.AndroidX.Compose/ComposableLambda0.cs:12` | no |
| `ComposableLambda0Int` | `IFunction0` | `src/Microsoft.AndroidX.Compose/ComposableLambda0Int.cs:17` | no |
| `ComposableLambda1` | `IFunction1` | `src/Microsoft.AndroidX.Compose/ComposableLambda1.cs:12` | no |
| `ComposableLambda2` | `IFunction2` | `src/Microsoft.AndroidX.Compose/ComposableLambda2.cs:13` | no |
| `ComposableLambda3` | `IFunction3` | `src/Microsoft.AndroidX.Compose/ComposableLambda3.cs:32` | no |
| `ComposableLambda4` | `IFunction4` | `src/Microsoft.AndroidX.Compose/ComposableLambda4.cs:17` | no |
| `DisposableEffectBody` | `IFunction1` | `src/Microsoft.AndroidX.Compose/DisposableEffectBody.cs:24` | no |
| `DragAndDropTarget` | `IDragAndDropTarget` | `src/Microsoft.AndroidX.Compose/DragAndDropTarget.cs:27` | no |
| `DrawerValueConfirmStateChange` | `IFunction1` | `src/Microsoft.AndroidX.Compose/DrawerValueConfirmStateChange.cs:31` | no |
| `IntCallback` | `IFunction1` | `src/Microsoft.AndroidX.Compose/IntCallback.cs:15` | no |
| `JobCompletionHandler` | `IFunction1` | `src/Microsoft.AndroidX.Compose/JobCompletionHandler.cs:16` | no |
| `LambdaViewModelFactory` | `ViewModelProvider.IFactory` | `src/Microsoft.AndroidX.Compose/LambdaViewModelFactory.cs:34` | no |
| `LaunchedEffectBody` | `IFunction2` | `src/Microsoft.AndroidX.Compose/LaunchedEffectBody.cs:48` | no |
| `LinkClickListener` | `AndroidX.Compose.UI.Text.ILinkInteractionListener` | `src/Microsoft.AndroidX.Compose/LinkClickListener.cs:14` | no |
| `ManagedBox` | `—` | `src/Microsoft.AndroidX.Compose/ManagedBox.cs:35` | no |
| `NavGraphBuilderLambda` | `IFunction1` | `src/Microsoft.AndroidX.Compose/NavGraphBuilderLambda.cs:23` | no |
| `NoOpSearchCallback` | `IFunction1` | `src/Microsoft.AndroidX.Compose/NoOpSearchCallback.cs:16` | no |
| `ObjectFunction0` | `IFunction0` | `src/Microsoft.AndroidX.Compose/ObjectFunction0.cs:18` | no |
| `OffsetCallback` | `IFunction1` | `src/Microsoft.AndroidX.Compose/OffsetCallback.cs:16` | yes |
| `OffsetPressCallback` | `IFunction3` | `src/Microsoft.AndroidX.Compose/OffsetPressCallback.cs:32` | yes |
| `PointerInputBlock` | `IFunction2` | `src/Microsoft.AndroidX.Compose/PointerInputBlock.cs:45` | yes |
| `RememberHolder` | `—` | `src/Microsoft.AndroidX.Compose/RememberHolder.cs:25` | no |
| `ShouldStartDragAndDropCallback` | `IFunction1` | `src/Microsoft.AndroidX.Compose/ShouldStartDragAndDropCallback.cs:11` | no |
| `SnapshotFlowContinuation` | `IContinuation` | `src/Microsoft.AndroidX.Compose/SnapshotFlowContinuation.cs:31` | no |
| `SuspendContinuation` | `IContinuation` | `src/Microsoft.AndroidX.Compose/SuspendContinuation.cs:47` | yes |

## Caveats

- **Heuristic, not semantic.** The classifier is regex-based. Member 
  detection covers methods, constructors, and property accessors but 
  may misattribute a JNI call inside an unusual member shape (lambda 
  bodies stored in fields, expression-bodied members spanning many 
  lines). Such calls land under a synthetic `(file-level)` row so no 
  call disappears.
- **`[ComposeBridge]` / `[ComposeFacade]` exclusion** is by attribute 
  presence on the immediately preceding lines. If a hand-written 
  member sits next to a generator-decorated one in the same file, both 
  classifications are emitted independently.
- **"Why not generated?"** is extracted from existing source comments 
  — XML doc `<remarks>` paragraphs containing _Why raw JNI_, 
  `// Why raw JNI:` lines, or adjacent `// ...` blocks. Entries that 
  show **TODO** are docs gaps in the source itself.
- **Call sites, not unique JVM symbols.** A cached `static IntPtr s_X` 
  bound once and used three times counts as three call sites.
- **Microsoft.AndroidX.Compose JCWs only.** The JCW table tracks `[Register("net/compose/…")]`, 
  the namespace this project owns. JCW shapes the Mono.Android binding 
  generator emits for upstream interfaces are not flagged.
