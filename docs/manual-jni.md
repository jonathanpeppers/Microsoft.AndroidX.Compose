# Manual JNI surface — Microsoft.AndroidX.Compose

Generated by `scripts/manual-jni-report.cs` on 2026-07-13 19:53 UTC.

Source of truth: `src/Microsoft.AndroidX.Compose/*.cs`. The script flags 
every C# member that calls `JNIEnv.*` directly, plus every Java 
Callable Wrapper the project registers under `net/compose/...`. 
Members whose bodies are emitted by the source generators 
(`[ComposeBridge]`, `[ComposeFacade]`) are deliberately excluded — 
the report measures **what's left for the generator to absorb**.

## Summary

- C# files scanned: **317**
- Files containing manual JNI or a JCW: **58**
- Manual `JNIEnv.*` call sites: **170**
- Lines inside flagged members: **1445**
- Java Callable Wrappers (`[Register("net/compose/…")]`): **37** (4 with raw JNI inside, 33 pure-managed delegate adapters)
- Generator-emitted `[ComposeBridge]` partials: **231**
- Generator-emitted `[ComposeFacade]` partials: **101**
- Manual JNI : generator-emitted ratio: **0.51** (`170` raw-JNI calls vs `332` generator-emitted partials)

## Per-file detail

### `src/Microsoft.AndroidX.Compose/Alignment.cs`

**Purpose.** C# wrapper around the androidx.compose.ui.Alignment singletons exposed by Kotlin's Alignment.Companion object. Each public static property forwards to the bound AndroidX.Compose.UI.IAlignment.Companion singleton and caches the wrapper fo...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `From` | method | 35–39 | 1 | Wrap the bound IAlignment peer's JNI handle in a brand-new Java.Lang.Object peer so casts to Java.Lang.Object on this instance return our own (stable) handle. Same approach as Shape. | TODO |

### `src/Microsoft.AndroidX.Compose/AndroidViewFactoryAdapter.cs`

**Purpose.** Function1&lt;Context, View&gt; JCW used by `AndroidView` as Compose's factory argument.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/AndroidViewUpdateAdapter.cs`

**Purpose.** Function1&lt;View, Unit&gt; JCW used by `AndroidView` as Compose's optional update argument.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/BrushBridges.cs`

**Purpose.** Hand-written JNI for the two Compose-graphics symbols the Mono.Android binder strips: - `androidx.compose.ui.graphics.Color.box-impl(J)Color` — the Kotlin-synthetic boxing factory that turns a packed `long` into a `Color` object. The `Co...

**Manual entry points** (3):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `BrushCompanion` | method | 35–54 | 3 | Lazy access to the `androidx.compose.ui.graphics.Brush$Companion` singleton — the binder exposes the type but not a public C# accessor for the Kotlin `Companion` static field, so we fetch it via JNI once and cache the bound peer. Subsequ... | TODO |
| `BoxColor` | method | 61–75 | 2 | Box a packed-long Color into a bound `BoundColor` peer via the Kotlin-synthetic `Color.box-impl(J)` static. Required because `BoundBrush.Companion.LinearGradient_mHitzGk(IList<Color>, ...)` (and every other gradient factory) takes a List... | TODO |
| `BrushSolidColor` | method | 94–105 | 2 | `new androidx.compose.ui.graphics.SolidColor(Color)` — the ctor is stripped because its parameter is a value-class `Color`. | TODO |

### `src/Microsoft.AndroidX.Compose/ComposableLambda0.cs`

**Purpose.** Function0&lt;Unit&gt; — zero-arg callbacks (Button.onClick, etc.). A [Register]'d Java.Lang.Object ACW so Compose's bytecode-typed lambda parameter accepts it.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda0Int.cs`

**Purpose.** Function0&lt;Integer&gt; — a no-arg Kotlin lambda returning a boxed `Integer`. Used for state-holder factories like rememberCarouselState(itemCount: () -&gt; Int) / rememberLazyListState(initialFirstVisibleItemIndex: () -&gt; Int) that r...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda1.cs`

**Purpose.** Function1&lt;T, Unit&gt; — single-arg callbacks (TextField's onValueChange, onCheckedChange, etc.). The body receives the raw Java arg and is responsible for unboxing it.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda2.cs`

**Purpose.** Function2&lt;Composer, Integer, Unit&gt; — top-level composition + theme/scope content. p0 is the composer, p1 is $changed.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda3.cs`

**Purpose.** Function3&lt;Scope, Composer, Integer, Unit&gt; — Column/Row/Box/Button content. p0 is the receiver scope (RowScope/ColumnScope), p1 is the composer, p2 is $changed. Three ctors: Action&lt;IComposer&gt; discards the scope — used wherever...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposableLambda4.cs`

**Purpose.** Function4&lt;Scope, T, Composer, Integer, Unit&gt; — the shape Compose's LazyListScope.items and LazyGridScope.items use for their per-item content lambda. p0 is the lazy item / grid item scope (raw `IntPtr`), p1 is the boxed per-item va...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ComposeBridges.cs`

**Purpose.** Unpack a Compose Size's width — high 32 bits as float.

_File also contributes 209 `[ComposeBridge]` + 98 `[ComposeFacade]` generator-emitted partials, excluded from this section._

**Manual entry points** (23):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `ModifierCompanionInstance` | method | 41–54 | 6 | TODO: document | TODO |
| `ModifierClipRoundedCorners` | method | 238–250 | 1 | Convenience wrapper that composes RoundedCornerShape + ModifierClip and manages the intermediate Shape local ref. Listed as out of scope in #36 — the underlying JNI calls are both generator-driven now; only the two-step orchestration sta... | see #36 |
| `BoxScopeAlign` | method | 2645–2658 | 3 | TODO: document | TODO |
| `BoxScopeMatchParentSize` | method | 2660–2673 | 3 | TODO: document | TODO |
| `RowScopeAlignVertical` | method | 2678–2691 | 3 | TODO: document | TODO |
| `ColumnScopeAlignHorizontal` | method | 2696–2709 | 3 | TODO: document | TODO |
| `FocusRequesterRequestFocus` | method | 2719–2728 | 3 | TODO: document | TODO |
| `ExposedDropdownMenu` | method | 3199–3251 | 3 | TODO: document | TODO |
| `RememberNavController` | method | 4070–4101 | 7 | TODO: document | TODO |
| `ModifierPointerInput` | method | 4159–4180 | 3 | androidx.compose.ui.input.pointer.SuspendingPointerInputFilterKt .pointerInput(Modifier, Object key1, PointerInputEventHandler): Modifier Hand-written because the [ComposeBridge] generator doesn't know about non-bound interface types lik... | TODO |
| `NewPointerInputEventHandler` | method | 4196–4212 | 3 | net.compose.PointerInputEventHandlerImpl.<init>(Function2) Allocates the Java-side helper (shipped via <AndroidJavaSource>) that implements the bound but otherwise-unreachable PointerInputEventHandler interface by forwarding invoke(scope... | TODO |
| `ConstraintsGetMinWidth` | method | 4294–4301 | 2 | TODO: document | TODO |
| `ConstraintsGetMaxWidth` | method | 4303–4310 | 2 | TODO: document | TODO |
| `ConstraintsGetMinHeight` | method | 4312–4319 | 2 | TODO: document | TODO |
| `ConstraintsGetMaxHeight` | method | 4321–4328 | 2 | TODO: document | TODO |
| `ConstraintsHasBoundedWidth` | method | 4330–4337 | 2 | TODO: document | TODO |
| `ConstraintsHasBoundedHeight` | method | 4339–4346 | 2 | TODO: document | TODO |
| `MeasurableMeasure` | method | 4379–4392 | 2 | TODO: document | TODO |
| `MeasureScopeLayout` | method | 4402–4426 | 3 | TODO: document | TODO |
| `MeasureScopeGetDensity` | method | 4438–4447 | 3 | TODO: document | TODO |
| `MeasureScopeGetFontScale` | method | 4449–4459 | 3 | TODO: document | TODO |
| `PlacementScopePlace` | method | 4469–4493 | 3 | TODO: document | TODO |
| `PlacementScopePlaceRelative` | method | 4495–4520 | 3 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/ComposeExtensions.Remember.cs`

**Purpose.** Compose's remember { factory() }, backed by the active composer's slot table. Returns the value of `factory` the first time this call site is reached, then the cached value on subsequent recompositions. The slot is keyed by `SourceLocati...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `RememberSaveableScalar` | method | 192–216 | 2 | TODO: document | TODO |
| `RememberSaveableWrapper` | method | 218–253 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/ComposeExtensions.TopAppBarDefaults.cs`

**Purpose.** Static factories mirroring Kotlin's androidx.compose.material3.TopAppBarDefaults singleton — each returns an `ITopAppBarScrollBehavior` configured for a particular collapse strategy. Surfaced as `IComposer` extensions so call sites read ...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `InvokeScrollBehavior` | method | 90–118 | 1 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/DateRangeSelectableDates.cs`

**Purpose.** SelectableDates adapter that gates calendar dates and years against an inclusive `MinUtcMillis` / `MaxUtcMillis` window plus an inclusive `MinYear` / `MaxYear` window. Designed to be allocated <strong>once per host instance</strong> as a...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/DiffSlotHolder.cs`

**Purpose.** Single-field `Object` wrapper that lets `DiffSlot{T}` stash a previous-render value (boxed when T is a value type) in Compose's slot table, which only holds `Object` references. Registered with the .NET-for-Android peer cache so the runt...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/DisposableEffectBody.cs`

**Purpose.** JCW for Compose's DisposableEffect body — Function1&lt;DisposableEffectScope, DisposableEffectResult&gt;. Kotlin invokes the body once per (key change | enter composition) and stores the returned `IDisposableEffectResult`; when keys chan...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/DragAndDropTarget.cs`

**Purpose.** androidx.compose.ui.draganddrop.DragAndDropTarget adapter — the receiver of drag-and-drop callbacks installed by `DragAndDropTarget(Func{DragAndDropEvent, bool}, DragAndDropTarget`. Mirrors Kotlin's DragAndDropTarget interface as a deleg...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/DragCallback.cs`

**Purpose.** JCW adapter implementing Function2&lt;PointerInputChange, Offset, Unit&gt; for the onDrag callback of detectDragGestures. The Kotlin signature passes the live PointerInputChange as p0 (which carries pointer id, position, pressure, etc.) ...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/DragGestureBlock.cs`

**Purpose.** JCW exposing a `IFunction2` that Compose's pointerInput(key, handler) invokes inside the gesture-detector coroutine, then forwards to detectDragGestures. Mirrors the shape of `PointerInputBlock` but routes to the drag-gesture suspend bri...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Invoke` | method | 59–115 | 2 | TODO: document | TODO |
| `EnsureSuspendedHandle` | method | 117–126 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/DrawerValueConfirmStateChange.cs`

**Purpose.** Function1&lt;DrawerValue, Boolean&gt; adapter used as the confirmStateChange callback when constructing a DrawerState via rememberDrawerState.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ImeAction.cs`

**Purpose.** C# accessor for Compose's soft-keyboard action enumeration — androidx.compose.ui.text.input.ImeAction. Surfaces every constant on the Kotlin ImeAction.Companion (Done, Search, Send, Go, Next, Previous, etc.) as a named int property so ca...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Companion` | method | 41–62 | 4 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/IntCallback.cs`

**Purpose.** JCW adapter implementing Function1&lt;Integer, Integer&gt;, used by the slideIn* / slideOut* transition factories whose offset parameter is a Kotlin (Int) -&gt; Int lambda mapping the container's measured width/height to the slide's init...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/JobCompletionHandler.cs`

**Purpose.** JCW for Function1&lt;Throwable?, Unit&gt; registered via `InvokeOnCompletion(bool, bool, IFunction1`. Compose invokes this handler when the launched coroutine's job transitions into the cancelling state, so we can propagate cancellation ...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/KeyboardOptionsCompanion.cs`

**Purpose.** Cached accessor for androidx.compose.foundation.text.KeyboardOptions.Companion — the singleton that exposes Default (the immutable KeyboardOptions instance shared by every Compose text field when callers haven't overridden the IME slot)....

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Get` | method | 41–62 | 4 | Resolve and cache the KeyboardOptions.Companion singleton. First call performs the JNI field lookup; subsequent calls return the cached wrapper. | TODO |

### `src/Microsoft.AndroidX.Compose/KeyboardType.cs`

**Purpose.** C# accessor for Compose's IME-type enumeration — androidx.compose.ui.text.input.KeyboardType. Surfaces every constant on the Kotlin KeyboardType.Companion (Text, Number, Phone, Uri, Email, Password, etc.) as a named int property so call ...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Companion` | method | 39–60 | 4 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/KotlinResult.cs`

**Purpose.** Raw-JNI helpers for Kotlin's kotlin.Result type — the boxed success/failure wrapper Kotlin coroutines pass through Continuation.resumeWith.

**Manual entry points** (5):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `CreateFailure` | method | 78–100 | 1 | Construct a kotlin.Result.Failure(throwable) instance — the boxed-failure form a `IContinuation` expects when something goes wrong inside its suspend body. The Java throwable to wrap; non-null. C# exceptions can be converted via `JavaPro... | TODO |
| `IsFailure` | method | 111–116 | 1 | Returns <see langword="true"/> if `handle` refers to a kotlin.Result.Failure instance — i.e. the resumed value represents a failure rather than a success. Raw JNI handle to test. `Zero` is treated as "not a failure" (a null resume value ... | TODO |
| `ExtractException` | method | 132–152 | 1 | Extract the underlying `Throwable` from a kotlin.Result.Failure peer. A peer wrapping a Result.Failure handle. Must have been vetted with `IsFailure(IntPtr` first. <returns> The wrapped throwable as a `Exception` (`Throwable` derives fro... | TODO |
| `EnsureResultKt` | method | 154–162 | 2 | TODO: document | TODO |
| `EnsureResultFailure` | method | 164–170 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/LambdaViewModelFactory.cs`

**Purpose.** Internal JCW adapter that exposes a `Func{T}` as an `IFactory` so `ViewModelProvider` can construct the view model the first time the host's `ViewModelStore` doesn't already contain the requested key.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/LaunchedEffectBody.cs`

**Purpose.** JCW for the suspend lambda Kotlin's LaunchedEffect expects: Function2&lt;CoroutineScope, Continuation&lt;in Unit&gt;, Any?&gt;. Bridges the Kotlin coroutine to a C# async Task body that accepts a `CancellationToken`.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/LinkClickListener.cs`

**Purpose.** androidx.compose.ui.text.LinkInteractionListener adapter that forwards Compose's link-tap callbacks to a C# `Action{T}`. Allocated once per `Clickable` call and held by the resulting `LinkAnnotation` so the JNI peer stays alive as long a...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ManagedBox.cs`

**Purpose.** Wraps an arbitrary managed value as a `Object` so it can cross the JNI boundary. Used by `CompositionLocal{T}` to hand pure-C# POCO values (themes, settings, custom contexts) to Kotlin's compositionLocalOf / staticCompositionLocalOf mach...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/Modifier.cs`

**Purpose.** C# mirror of Kotlin's androidx.compose.ui.Modifier chain. Build a chain by calling a static factory on this type — each call returns a NEW immutable `Modifier` with the op appended. Chain further ops via the matching extension methods in...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Build` | method | 406–444 | 3 | Materialize the chain into a managed IModifier wrapper, optionally prepending a Modifier.padding(contentPadding) op against the supplied `IntPtr` handle. The padding op runs FIRST, before any user ops — semantically equivalent to Modifie... | see #46 |

### `src/Microsoft.AndroidX.Compose/ModifierExtensions.cs`

**Purpose.** Fluent chain helpers for `Modifier`. Each method is an extension on `Modifier` so callers can write both Modifier.Padding(16).FillMaxWidth() (the static factory on `Modifier` kicks off the chain, then these extensions continue it) and so...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Border` | method | 232–254 | 1 | Modifier.border(width, color, RoundedCornerShape(cornerRadius)) — draws a stroke with rounded corners. Match `cornerRadius` to a `Clip(Dp` earlier in the chain so the corners align (otherwise the rectangular stroke gets sliced by a round... | TODO |
| `AppendPointerInput` | method | 1440–1470 | 1 | Shared plumbing for DetectTapGestures / DetectDragGestures / DetectTransformGestures: resolve the user-supplied `key` to a Java object (Kotlin uses reference equality for the default- overload form, so we want a stable JNI object for the... | TODO |

### `src/Microsoft.AndroidX.Compose/MutableComposableLambda0.cs`

**Purpose.** Identity-stable variant of `ComposableLambda0` for use with `IComposer, Action, int, string`. The wrapper is allocated once per call site and cached in the composer's slot table; subsequent renders rebind `Target` instead of allocating a...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/MutableComposableLambda1.cs`

**Purpose.** Identity-stable variant of `ComposableLambda1` for use with `Object?}, int, string`. The wrapper is allocated once per call site and cached in the composer's slot table; subsequent renders rebind `Target` instead of allocating a fresh JC...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/NavGraphBuilderLambda.cs`

**Purpose.** Function1&lt;NavGraphBuilder, Unit&gt; — the builder argument to NavHostKt.NavHost(navController, startDestination, modifier, route, builder, ...). NOT a @Composable lambda: NavHost invokes it synchronously during graph construction, bef...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/NoOpSearchCallback.cs`

**Purpose.** Function1&lt;String, Unit&gt; stub that ignores its argument and returns null. Supplied to SearchBarDefaults.InputField.onSearch when the C# caller did not provide an onSearch callback — the Kotlin API does not tolerate a null callback (...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ObjectFunction0.cs`

**Purpose.** JCW that implements Kotlin's Function0&lt;Object&gt; — a zero-argument lambda returning a boxed managed object. Unlike `ComposableLambda0`, which returns Unit.INSTANCE (void in Kotlin terms), this one returns a typed value Compose / the ...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/Offset.cs`

**Purpose.** C# wrapper around Kotlin's androidx.compose.ui.geometry.Offset.

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `(file-level)` | init | 68–71 | 3 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/OffsetCallback.cs`

**Purpose.** JCW adapter implementing Function1&lt;Offset, Unit&gt; for gesture-detector callbacks like onTap / onLongPress / onDoubleTap. Because Kotlin's Offset is a @JvmInline value class, the boxed argument p0 arrives as an androidx.compose.ui.ge...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/OffsetPressCallback.cs`

**Purpose.** JCW adapter implementing Function3&lt;PressGestureScope, Offset, Continuation, Object&gt; for the onPress callback of detectTapGestures. Kotlin lowers the original suspend PressGestureScope.(Offset) -&gt; Unit shape to a Function3 with t...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/PaddingValues.cs`

**Purpose.** C# wrapper around androidx.compose.foundation.layout.PaddingValues. Models inset / asymmetric padding to apply inside scroll containers (`LazyColumn{T}`, `LazyRow{T}`, the lazy grids and pagers) and the Button family's contentPadding slot.

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `BuildHandle` | method | 88–89 | 1 | The bound PaddingKt.PaddingValues(...) factories return an IPaddingValues whose underlying JNI handle is the freshly- constructed peer. We NewLocalRef + TransferLocalRef into the base class so the wrapper owns its own reference (the temp... | TODO |

### `src/Microsoft.AndroidX.Compose/PointerInputBlock.cs`

**Purpose.** JCW exposing a `IFunction2` that Compose's pointerInput(key, handler) invokes inside the gesture-detector coroutine. The Java-side net.compose.PointerInputEventHandlerImpl (shipped via &lt;AndroidJavaSource&gt;) implements the bound, but...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Invoke` | method | 67–134 | 2 | TODO: document | TODO |
| `EnsureSuspendedHandle` | method | 136–145 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/RememberHolder.cs`

**Purpose.** Single-field `Object` wrapper that lets `Remember{T}(Func{T}, int, string` store an arbitrary managed T (often a `MutableState{T}`) in Compose's slot table, which only holds `Object` references. Registered with the .NET-for-Android peer ...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/SaveableBridges.cs`

**Purpose.** Hand-written raw-JNI bridges for the `androidx.compose.runtime.saveable` package. The Xamarin NuGet (`Xamarin.AndroidX.Compose.Runtime.Saveable.Android`) ships the Java library but does not generate C# bindings, so every Saveable entry p...

**Manual entry points** (5):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `SaverAutoSaver` | method | 36–60 | 5 | androidx.compose.runtime.saveable.SaverKt.autoSaver(): Saver<T, Object> The default `Saver` Compose uses for any `Bundle`-saveable value (primitives, strings, parcelables, …). Cached as a global ref — returns the same singleton on every ... | TODO |
| `EmptyObjectArray` | method | 68–84 | 3 | Lazily-cached empty `Object[]` for the `vararg inputs` parameter of `rememberSaveable`. Compose treats inputs as "dependency tracking" — if any element changes, the cached factory result is discarded. We always pass an empty array (= "ne... | TODO |
| `RememberSaveableSimple` | method | 95–128 | 3 | androidx.compose.runtime.saveable.RememberSaveableKt.rememberSaveable( Object[] inputs, Function0<? extends T> init, Composer composer, int $changed): Object The no-saver overload — Compose internally uses `autoSaver()`, which handles ev... | TODO |
| `RememberSaveableMutableState` | method | 145–180 | 3 | androidx.compose.runtime.saveable.RememberSaveableKt.rememberSaveable( Object[] inputs, Saver<T, ?> stateSaver, Function0<? extends MutableState<T>> init, Composer composer, int $changed): MutableState<T> The MutableState overload — Comp... | TODO |
| `BuildKeysArray` | method | 194–220 | 3 | Builds a Kotlin-compatible `Object[]` from a managed `object?[]` for the `inputs` (vararg) parameter of `rememberSaveable`. Each element is boxed via the same primitive→Java.Lang.Object switch MutableState<T> uses, so primitives compare ... | TODO |

### `src/Microsoft.AndroidX.Compose/ShapesBridges.cs`

**Purpose.** Build a Material 3 `Shapes` with per-slot overrides. Each `IntPtr` is either a CornerBasedShape JNI handle to use for that slot, or `Zero` to fall back to the M3 baseline default for that slot via Kotlin's synthetic $default mechanism.

_File also contributes 1 `[ComposeBridge]` generator-emitted partials, excluded from this section._

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `BuildShapes` | method | 46–71 | 3 | Build a Material 3 `Shapes` with per-slot overrides. Each `IntPtr` is either a CornerBasedShape JNI handle to use for that slot, or `Zero` to fall back to the M3 baseline default for that slot via Kotlin's synthetic $default mechanism. 5... | TODO |

### `src/Microsoft.AndroidX.Compose/SheetValueConfirmStateChange.cs`

**Purpose.** Function1&lt;SheetValue, Boolean&gt; adapter used as the confirmValueChange callback when constructing a SheetState via rememberModalBottomSheetState / rememberStandardBottomSheetState.

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/ShouldStartDragAndDropCallback.cs`

**Purpose.** JCW adapter for the `shouldStartDragAndDrop: (DragAndDropEvent) -> Boolean` parameter on `Modifier.dragAndDropTarget(...)`. Wraps a managed `Func<DragAndDropEvent, bool>` and returns a boxed Java.Lang.Boolean so Kotlin's Function1 contra...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/SnapshotFlowContinuation.cs`

**Purpose.** JCW implementing Kotlin's `IContinuation` for the long-running Flow.collect call that drives `SnapshotFlow{T}(Func{T}`. Unlike `SuspendContinuation`, which models a one-shot suspend resumed back into a `TaskCompletionSource{TResult}`, th...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/SuspendBridge.cs`

**Purpose.** Shared plumbing for calling Kotlin suspend functions from C# async/await. Each call allocates one `SuspendContinuation`, hands it to the Kotlin function, and either: completes synchronously when Kotlin returns anything other than the COR...

**Manual entry points** (3):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Invoke` | method | 74–157 | 1 | Call a Kotlin suspend function and project its boxed result through `unbox` into a typed `Task{TResult}`. Invokes the Kotlin suspend function with the supplied `SuspendContinuation` as the trailing argument and returns either the boxed r... | TODO |
| `IsCoroutineSuspended` | method | 202–207 | 1 | TODO: document | TODO |
| `EnsureSuspendedHandle` | method | 209–218 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/SuspendBridges.cs`

**Purpose.** JNI bridges for Kotlin `suspend` functions. Bodies are emitted by ComposeBridgeGenerator from `[ComposeBridge(Suspend = true)]`. The trailing `IContinuation cont` parameter is detected as a Kotlin continuation slot; the generator takes c...

_File also contributes 16 `[ComposeBridge]` generator-emitted partials, excluded from this section._

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `AndroidUiDispatcherMain` | method | 224–255 | 9 | androidx.compose.ui.platform.AndroidUiDispatcher.Companion.getMain(): kotlin.coroutines.CoroutineContext Returns the singleton CoroutineContext that combines a main-thread dispatcher with a MonotonicFrameClock backed by Choreographer. Re... | TODO |

### `src/Microsoft.AndroidX.Compose/SuspendContinuation.cs`

**Purpose.** Java Callable Wrapper that lets a Kotlin suspend function resume back into managed code via a `TaskCompletionSource{TResult}`. The wrapped kotlin.Result&lt;T&gt; handed to `Object` is stored verbatim and unboxed by `SuspendBridge`.

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `CompleteWithLocalHandle` | method | 172–195 | 2 | Funnel a raw JNI local ref into the TCS. Promotes the handle to a global ref, wraps it in a managed peer with `TransferGlobalRef`, and stores the peer in `Tcs`. Local ref to the boxed result, or IntPtr.Zero. When true, the supplied local... | TODO |

### `src/Microsoft.AndroidX.Compose/TextFieldValueBridges.cs`

**Purpose.** JNI bridge for the one piece of androidx.compose.ui.text.input.TextFieldValue that the binder strips: the constructor. Everything else — Text/Selection/Composition/AnnotatedString properties, Copy(text, selection, composition) overloads,...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `EnsureTextFieldValueClass` | method | 38–44 | 2 | TODO: document | TODO |
| `NewTextFieldValueImpl` | method | 49–71 | 3 | Construct a Kotlin TextFieldValue and return the bound binding wrapper. Always invokes the synthetic default-mask ctor so we can pass null for composition without knowing which non-synthetic overloads exist. | TODO |

### `src/Microsoft.AndroidX.Compose/TextStyleCompanion.cs`

**Purpose.** Cached accessor for androidx.compose.ui.text.TextStyle.Companion — the singleton that exposes Default (and via that Default.toSpanStyle()). The C# binding doesn't expose a static accessor for the Companion field, so we read it through JN...

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Get` | method | 19–40 | 4 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/ThemeBridges.cs`

**Purpose.** Build a Material 3 light `ColorScheme` using every per-slot default from the M3 tonal palette tokens.

_File also contributes 1 `[ComposeBridge]` generator-emitted partials, excluded from this section._

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `InvokeColorSchemeDefault` | method | 98–139 | 3 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/TransformGestureBlock.cs`

**Purpose.** JCW exposing a `IFunction2` that Compose's pointerInput(key, handler) invokes inside the gesture-detector coroutine, then forwards to detectTransformGestures. Mirrors the shape of `PointerInputBlock` but routes to the pinch / zoom / rota...

**Manual entry points** (2):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `Invoke` | method | 37–88 | 2 | TODO: document | TODO |
| `EnsureSuspendedHandle` | method | 90–99 | 2 | TODO: document | TODO |

### `src/Microsoft.AndroidX.Compose/TransformGestureCallback.cs`

**Purpose.** JCW adapter implementing Function4&lt;Offset, Offset, Float, Float, Unit&gt; for the onGesture callback of detectTransformGestures. Kotlin passes the gesture centroid in p0, the cumulative pan in p1, the cumulative zoom multiplier in p2,...

_No hand-written JNI members; JCW(s) only — see table below._

### `src/Microsoft.AndroidX.Compose/TypographyBridges.cs`

**Purpose.** Build a Material 3 `Typography` with per-slot overrides. Each `TextStyle` is either a wrapper to use for that slot, or null to fall back to the M3 baseline default for that slot via Kotlin's synthetic $default mechanism.

**Manual entry points** (1):

| Member | Kind | Lines | JNI calls | Why not generated? | Migration |
| --- | --- | --- | --- | --- | --- |
| `BuildTypography` | method | 50–123 | 3 | Build a Material 3 `Typography` with per-slot overrides. Each `TextStyle` is either a wrapper to use for that slot, or null to fall back to the M3 baseline default for that slot via Kotlin's synthetic $default mechanism. 15-bit mask: bit... | TODO |

### `src/Microsoft.AndroidX.Compose/UnitCallback.cs`

**Purpose.** JCW adapter implementing Function0&lt;Unit&gt; for gesture callbacks that take no arguments — Compose's onDragEnd / onDragCancel on detectDragGestures.

_No hand-written JNI members; JCW(s) only — see table below._

## JCW classes

Every Microsoft.AndroidX.Compose Java Callable Wrapper — classes registered under 
the project's own `net/compose/` JNI namespace. They lower C# delegates 
to Kotlin `IFunctionN` shapes the binding generator can't synthesize.

| Class | Implements | File:line | Has raw JNI? |
| --- | --- | --- | --- |
| `AndroidViewFactoryAdapter` | `IFunction1` | `src/Microsoft.AndroidX.Compose/AndroidViewFactoryAdapter.cs:13` | no |
| `AndroidViewUpdateAdapter` | `IFunction1` | `src/Microsoft.AndroidX.Compose/AndroidViewUpdateAdapter.cs:13` | no |
| `ComposableLambda0` | `IFunction0` | `src/Microsoft.AndroidX.Compose/ComposableLambda0.cs:12` | no |
| `ComposableLambda0Int` | `IFunction0` | `src/Microsoft.AndroidX.Compose/ComposableLambda0Int.cs:17` | no |
| `ComposableLambda1` | `IFunction1` | `src/Microsoft.AndroidX.Compose/ComposableLambda1.cs:12` | no |
| `ComposableLambda2` | `IFunction2` | `src/Microsoft.AndroidX.Compose/ComposableLambda2.cs:13` | no |
| `ComposableLambda3` | `IFunction3` | `src/Microsoft.AndroidX.Compose/ComposableLambda3.cs:32` | no |
| `ComposableLambda4` | `IFunction4` | `src/Microsoft.AndroidX.Compose/ComposableLambda4.cs:17` | no |
| `DateRangeSelectableDates` | `ISelectableDates` | `src/Microsoft.AndroidX.Compose/DateRangeSelectableDates.cs:28` | no |
| `DiffSlotHolder` | `—` | `src/Microsoft.AndroidX.Compose/DiffSlotHolder.cs:22` | no |
| `DisposableEffectBody` | `IFunction1` | `src/Microsoft.AndroidX.Compose/DisposableEffectBody.cs:24` | no |
| `DragAndDropTarget` | `IDragAndDropTarget` | `src/Microsoft.AndroidX.Compose/DragAndDropTarget.cs:27` | no |
| `DragCallback` | `IFunction2` | `src/Microsoft.AndroidX.Compose/DragCallback.cs:29` | no |
| `DragGestureBlock` | `IFunction2` | `src/Microsoft.AndroidX.Compose/DragGestureBlock.cs:38` | yes |
| `DrawerValueConfirmStateChange` | `ConfirmStateChangeAdapter<DrawerValue>` | `src/Microsoft.AndroidX.Compose/DrawerValueConfirmStateChange.cs:24` | no |
| `IntCallback` | `IFunction1` | `src/Microsoft.AndroidX.Compose/IntCallback.cs:15` | no |
| `JobCompletionHandler` | `IFunction1` | `src/Microsoft.AndroidX.Compose/JobCompletionHandler.cs:16` | no |
| `LambdaViewModelFactory` | `ViewModelProvider.IFactory` | `src/Microsoft.AndroidX.Compose/LambdaViewModelFactory.cs:33` | no |
| `LaunchedEffectBody` | `IFunction2` | `src/Microsoft.AndroidX.Compose/LaunchedEffectBody.cs:49` | no |
| `LinkClickListener` | `AndroidX.Compose.UI.Text.ILinkInteractionListener` | `src/Microsoft.AndroidX.Compose/LinkClickListener.cs:14` | no |
| `ManagedBox` | `—` | `src/Microsoft.AndroidX.Compose/ManagedBox.cs:34` | no |
| `MutableComposableLambda0` | `IFunction0` | `src/Microsoft.AndroidX.Compose/MutableComposableLambda0.cs:19` | no |
| `MutableComposableLambda1` | `IFunction1` | `src/Microsoft.AndroidX.Compose/MutableComposableLambda1.cs:17` | no |
| `NavGraphBuilderLambda` | `IFunction1` | `src/Microsoft.AndroidX.Compose/NavGraphBuilderLambda.cs:23` | no |
| `NoOpSearchCallback` | `IFunction1` | `src/Microsoft.AndroidX.Compose/NoOpSearchCallback.cs:16` | no |
| `ObjectFunction0` | `IFunction0` | `src/Microsoft.AndroidX.Compose/ObjectFunction0.cs:18` | no |
| `OffsetCallback` | `IFunction1` | `src/Microsoft.AndroidX.Compose/OffsetCallback.cs:15` | no |
| `OffsetPressCallback` | `IFunction3` | `src/Microsoft.AndroidX.Compose/OffsetPressCallback.cs:31` | no |
| `PointerInputBlock` | `IFunction2` | `src/Microsoft.AndroidX.Compose/PointerInputBlock.cs:46` | yes |
| `RememberHolder` | `—` | `src/Microsoft.AndroidX.Compose/RememberHolder.cs:25` | no |
| `SheetValueConfirmStateChange` | `ConfirmStateChangeAdapter<SheetValue>` | `src/Microsoft.AndroidX.Compose/SheetValueConfirmStateChange.cs:25` | no |
| `ShouldStartDragAndDropCallback` | `IFunction1` | `src/Microsoft.AndroidX.Compose/ShouldStartDragAndDropCallback.cs:11` | no |
| `SnapshotFlowContinuation` | `IContinuation` | `src/Microsoft.AndroidX.Compose/SnapshotFlowContinuation.cs:29` | no |
| `SuspendContinuation` | `IContinuation` | `src/Microsoft.AndroidX.Compose/SuspendContinuation.cs:46` | yes |
| `TransformGestureBlock` | `IFunction2` | `src/Microsoft.AndroidX.Compose/TransformGestureBlock.cs:24` | yes |
| `TransformGestureCallback` | `IFunction4` | `src/Microsoft.AndroidX.Compose/TransformGestureCallback.cs:22` | no |
| `UnitCallback` | `IFunction0` | `src/Microsoft.AndroidX.Compose/UnitCallback.cs:23` | no |

## Caveats

- **Heuristic, not semantic.** The classifier is regex-based. Member 
  detection covers methods, constructors, and property accessors but 
  may misattribute a JNI call inside an unusual member shape (lambda 
  bodies stored in fields, expression-bodied members spanning many 
  lines). Such calls land under a synthetic `(file-level)` row so no 
  call disappears.
- **`[ComposeBridge]` / `[ComposeFacade]` exclusion** is by attribute 
  presence on the immediately preceding lines. If a hand-written 
  member sits next to a generator-decorated one in the same file, both 
  classifications are emitted independently.
- **"Why not generated?"** is extracted from existing source comments 
  — XML doc `<remarks>` paragraphs containing _Why raw JNI_, 
  `// Why raw JNI:` lines, or adjacent `// ...` blocks. Entries that 
  show **TODO** are docs gaps in the source itself.
- **Call sites, not unique JVM symbols.** A cached `static IntPtr s_X` 
  bound once and used three times counts as three call sites.
- **Microsoft.AndroidX.Compose JCWs only.** The JCW table tracks `[Register("net/compose/…")]`, 
  the namespace this project owns. JCW shapes the Mono.Android binding 
  generator emits for upstream interfaces are not flagged.
