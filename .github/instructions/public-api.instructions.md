---
applyTo: "**/PublicAPI.*.txt"
---

# Public API baseline instructions

Always keep public API entries alphabetically sorted using ordinal string
ordering. Preserve `#nullable enable` as the first line, and sort every
subsequent non-empty line whenever adding, removing, or updating an entry.
