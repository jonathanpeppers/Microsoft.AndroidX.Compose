namespace ComposeNet.Samples.Reply;

/// <summary>
/// An enum to define different types of email folders or categories.
/// Port of upstream's <c>MailboxType</c>.
/// </summary>
public enum MailboxType
{
    Inbox,
    Drafts,
    Sent,
    Spam,
    Trash,
}
