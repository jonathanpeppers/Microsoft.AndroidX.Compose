namespace Microsoft.AndroidX.Compose;

/// <summary>
/// Material 3 <c>MediumTopAppBar</c> — a two-row top app bar that
/// collapses to a single row as the underlying scroll behavior
/// progresses. Same slots as <see cref="TopAppBar"/>. Typically paired
/// with <see cref="TopAppBarDefaults.ExitUntilCollapsedScrollBehavior(global::AndroidX.Compose.Material3.TopAppBarState, int, string)"/>
/// on its <c>ScrollBehavior</c> slot.
/// </summary>
public sealed partial class MediumTopAppBar;
