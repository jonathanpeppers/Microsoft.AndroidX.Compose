namespace Microsoft.AndroidX.Compose.Maui.Sample.Pages;

/// <summary>
/// WebView demo — verifies the stock <c>WebViewHandler</c> +
/// <c>MauiWebView</c> (an Android <c>WebView</c> subclass) renders
/// through Compose <see cref="AndroidX.Compose.AndroidView"/> with
/// JS execution intact.
/// </summary>
public partial class WebViewPage : ContentPage
{
    /// <summary>Build the page.</summary>
    public WebViewPage()
    {
        InitializeComponent();
    }
}
