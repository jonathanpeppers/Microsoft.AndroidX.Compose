<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Microsoft.AndroidX.Compose.Maui.Sample.Pages.ButtonsPage"
             Title="Buttons">

    <!--
        Exercises every mapper on `ButtonHandler`:
            * Text                       -> MapText
            * TextColor                  -> MapTextColor (Compose contentColor)
            * Background (SolidPaint)    -> MapBackground (Compose containerColor)
            * HorizontalLayoutAlignment  -> MapHorizontalLayoutAlignment
              (toggles `Modifier.fillMaxWidth()`).
    -->
    <ScrollView>
        <VerticalStackLayout
            Padding="30,30"
            Spacing="20">

            <Label
                Text="Buttons"
                Style="{StaticResource Headline}" />

            <Label
                Text="-- Default vs themed colors --"
                FontAttributes="Bold" />

            <!-- Hugs content; default M3 colors. -->
            <Button Text="Default (hugs content)"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Button Text="MAUI Primary"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Button Text="Custom container + content"
                    BackgroundColor="#FFCDD2"
                    TextColor="#B71C1C"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Label
                Text="-- HorizontalOptions=Fill (Modifier.fillMaxWidth) --"
                FontAttributes="Bold" />

            <Button Text="Filled width"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    HorizontalOptions="Fill"
                    Clicked="OnButtonClicked" />

            <Button Text="Filled, default M3 colors"
                    HorizontalOptions="Fill"
                    Clicked="OnButtonClicked" />

            <Label
                Text="-- Click count --"
                FontAttributes="Bold" />

            <Label
                x:Name="ClickCountLabel"
                Text="No clicks yet" />

            <!-- New mapper coverage from the keys-fill PR. -->
            <Label
                Text="-- New mapper coverage --"
                FontAttributes="Bold" />

            <Label Text="CharacterSpacing — letters spread out." />
            <Button Text="Wide letters"
                    CharacterSpacing="6"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Label Text="CornerRadius — pill shape (50)." />
            <Button Text="Pill"
                    CornerRadius="50"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Label Text="CornerRadius — sharp corners (0)." />
            <Button Text="Sharp"
                    CornerRadius="0"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Label Text="Font — large bold." />
            <Button Text="Big bold"
                    FontSize="24"
                    FontAttributes="Bold"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Label Text="Padding — generous internal space." />
            <Button Text="Padded"
                    Padding="40,20"
                    BackgroundColor="#FFCDD2"
                    TextColor="#B71C1C"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Label Text="StrokeColor + StrokeThickness — outlined button." />
            <Button Text="Outlined"
                    BackgroundColor="Transparent"
                    TextColor="#1976D2"
                    BorderColor="#1976D2"
                    BorderWidth="2"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Label Text="StrokeColor + StrokeThickness — heavy stroke." />
            <Button Text="Heavy outline"
                    BackgroundColor="Transparent"
                    TextColor="#E91E63"
                    BorderColor="#E91E63"
                    BorderWidth="4"
                    CornerRadius="20"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

            <Label Text="ImageSource — leading icon (dotnet_bot)." />
            <Button Text="With image"
                    ImageSource="dotnet_bot.png"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    HorizontalOptions="Start"
                    Clicked="OnButtonClicked" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
