namespace ComposeNet;

/// <summary>
/// Material 3 <c>ElevatedButton</c> — same call shape as
/// <see cref="Button"/> but uses shadow elevation instead of a
/// tonal fill. Useful when separation from the background needs to
/// read as a lifted surface.
/// </summary>
public sealed partial class ElevatedButton;
