namespace ComposeNet;

/// <summary>
/// Material 3 Badge. A small colored marker, typically dropped
/// inside a <see cref="BadgedBox"/>.Badge slot. Children are the
/// badge content (usually a count), laid out in a RowScope. Pass
/// no children for a plain dot.
/// </summary>
public sealed partial class Badge;
