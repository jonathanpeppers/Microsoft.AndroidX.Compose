namespace ComposeNet;

/// <summary>
/// Material 3 non-clickable <c>Card</c> — a tonal surface with
/// rounded corners that lays its children out as a
/// <see cref="Column"/>. Children are added via collection-initializer
/// syntax:
/// <code>
/// new Card { new Text("Title"), new Text("Subtitle") }
/// </code>
/// </summary>
public sealed partial class Card;
