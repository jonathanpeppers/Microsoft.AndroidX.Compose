namespace ComposeNet;

/// <summary>
/// Material 3 <c>CenterAlignedTopAppBar</c> — same shape as
/// <see cref="TopAppBar"/> but centers the <c>Title</c> horizontally
/// between the navigation icon and actions. Supports the same
/// <c>ScrollBehavior</c> slot — see <see cref="TopAppBar"/> for the
/// pairing with <see cref="Modifier.NestedScroll(NestedScrollConnection)"/>.
/// </summary>
public sealed partial class CenterAlignedTopAppBar;
